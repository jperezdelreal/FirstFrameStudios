# Assets

First Punch uses **procedural Canvas 2D drawing** for all visual art. Characters, backgrounds, UI elements, and effects are rendered in real-time using the HTML5 Canvas API — no external image files are needed.

This directory exists as a conventional project landmark. If bitmap or vector assets are ever introduced, they belong here.
